"use client";
import { useState } from "react"; import Link from "next/link"; import useAuth from "@/features/auth/hooks/useAuth";
export default function LostPasswordPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const { forgotPassword, loading, error } = useAuth(); const submit = async (e) => { e.preventDefault(); try { setMessage((await forgotPassword(email)).message); } catch {} }; return <section><div className="container pt-43 pb-25 max-w-xl mx-auto px-5"><h1 className="texts_16_medium text-head tracking-widest mb-5">RESET PASSWORD</h1><p className="texts_14_regular text-second mb-6">Enter your email and we&apos;ll send you a reset link.</p>{message && <p className="text-green-700 text-sm mb-4">{message}</p>}{error && <p className="text-red-500 text-sm mb-4">{error}</p>}<form onSubmit={submit} className="flex flex-col gap-5"><input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="Email address *" className="border border-footer px-4 py-4" /><button disabled={loading} className="bg-head text-white py-4">{loading ? "SENDING..." : "SEND RESET LINK"}</button></form><Link href="/login-register" className="block mt-6 underline text-sm">Back to login</Link></div></section> }
