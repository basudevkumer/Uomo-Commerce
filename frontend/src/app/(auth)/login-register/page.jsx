import RegiLog from "@/features/auth/components/regiLog/RegiLog";

export const metadata = {
  title: "Login / Register | Uomo",
  description: "Sign in or create account",
};

export default function LoginRegisterPage() {
  return <RegiLog />;
}
